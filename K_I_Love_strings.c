#include <stdio.h>
int main()
{
  int n;

  scanf("%d", &n);
  for (int i = 0; i < n; i++)
  {
    char s[51];
    char s2[51];
    scanf("%s %s", &s, &s2);
    int j = 0;
    while (true)
    {
      if (s[j] == '\0' && s2[j] == '\0')
      {
        break;
      }
      else if ()
      {
      }
    }
  }

  return 0;
}